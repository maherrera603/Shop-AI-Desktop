<UserControl x:Class="ShopAIDesktop.UI.Components.CustomCard.CustomCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShopAIDesktop.UI.Components.CustomCard"
             xmlns:svgc="clr-namespace:SharpVectors.Converters;assembly=SharpVectors.Converters.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="#4C1D95"
            CornerRadius="10"
            Padding="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12"
                              ShadowDepth="2"
                              Opacity="0.10" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#FFFFFF"
                           VerticalAlignment="Center" />

                <Border Width="42"
                        Height="42"
                        CornerRadius="10"
                        Background="Transparent"
                        HorizontalAlignment="Right"
                >
                    <svgc:SvgViewbox Source="{Binding Icon, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                </Border>
                
            </Grid>


            <TextBlock Grid.Row="1"
                       Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="#FFFFFF"
                       Margin="0, 5,0,0" />


            <TextBlock Grid.Row="2"
               Text="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}}"
               FontSize="12"
               Foreground="#FFFFFF"
               Margin="0,8,0,0" />


        </Grid>
           
    </Border>
</UserControl>
